{%- liquid
  assign item_count = section.blocks.size
  if item_count == 0
    assign item_count = 5
  endif
-%}
<section class="trust">
  <div class="trust-row" style="--count:{{ item_count }}">
    {%- for block in section.blocks -%}
      {%- assign t_label = 'sections.trust_strip.' | append: block.id | append: '.label' | t -%}
      <div class="trust-item" {{ block.shopify_attributes }}>
        {%- render 'icon', icon: block.settings.icon -%}
        <span class="t">{%- if t_label contains 'ranslation missing' -%}{{ block.settings.label }}{%- else -%}{{ t_label }}{%- endif -%}</span>
      </div>
    {%- else -%}
      <!-- Default trust items (shown until blocks are configured) -->
      <div class="trust-item">{%- render 'icon', icon: 'clump' -%}<span class="t">Ultra Clumping</span></div>
      <div class="trust-item">{%- render 'icon', icon: 'dust' -%}<span class="t">99.9% Dust-Free</span></div>
      <div class="trust-item">{%- render 'icon', icon: 'absorb' -%}<span class="t">Super Absorbent</span></div>
      <div class="trust-item">{%- render 'icon', icon: 'odor' -%}<span class="t">Odour Control</span></div>
      <div class="trust-item">{%- render 'icon', icon: 'mineral' -%}<span class="t">Natural Mineral</span></div>
    {%- endfor -%}
  </div>
</section>

{% schema %}
{
  "name": "Trust Strip",
  "tag": "div",
  "blocks": [
    {
      "type": "item",
      "name": "Trust item",
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "clump", "label": "Clumping" },
            { "value": "dust", "label": "Dust" },
            { "value": "absorb", "label": "Absorbency" },
            { "value": "odor", "label": "Odour" },
            { "value": "mineral", "label": "Mineral" },
            { "value": "leaf", "label": "Leaf" },
            { "value": "shield", "label": "Shield" },
            { "value": "sparkle", "label": "Sparkle" }
          ],
          "default": "clump"
        },
        {
          "type": "text",
          "id": "label",
          "label": "Label",
          "default": "Ultra Clumping"
        }
      ]
    }
  ],
  "presets": [{ "name": "Trust Strip" }]
}
{% endschema %}
